/**
 * Receivable module — derived from Fees (Payment) and Payments (Receipt).
 * No separate database table. All calculations are read-only.
 */

export * from './types';
export * from './receivable-service';
